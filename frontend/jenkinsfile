pipeline {
    agent any

    environment {
        NODE_ENV = 'production'
    }

    stages {
        stage('Declarative: Checkout SCM') {
            steps {
                script {
                    checkout scm
                }
            }
        }

        stage('Install dependencies') {
            steps {
                script {
                    // Installer toutes les dépendances, y compris celles de développement
                    dir('frontend') {
                        sh 'npm install --legacy-peer-deps' // Ajout de --legacy-peer-deps si nécessaire
                    }
                }
            }
        }

        stage('Unit Test') {
            steps {
                script {
                    // Exécuter les tests en passant à l'étape suivante si aucun test n'est trouvé
                    dir('frontend') {
                        sh 'npm test -- --passWithNoTests'
                    }
                }
            }
        }

        stage('Build application') {
            steps {
                script {
                    dir('frontend') {
                        sh 'npm run build'
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    echo 'Déploiement de l\'application'
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline terminé.'
        }
        success {
            echo 'Le pipeline a réussi !'
        }
        failure {
            echo 'Le pipeline a échoué.'
        }
    }
}

pipeline {
    agent any

    environment {
        NODE_ENV = 'production'
    }

    stages {
        stage('Declarative: Checkout SCM') {
            steps {
                script {
                    checkout scm
                }
            }
        }

        stage('Clean and Install dependencies') {
            steps {
                script {
                    // Assurer une installation propre des modules Node
                    dir('frontend') {
                        // Remove the node_modules and install dependencies with npm ci
                        sh 'rm -rf node_modules'
                        sh 'npm install' // Use `npm install` instead of `npm ci` to ensure dependencies are correctly installed
                    }
                }
            }
        }

        stage('Unit Test') {
            steps {
                script {
                    // Exécuter les tests en passant à l'étape suivante si aucun test n'est trouvé
                    dir('frontend') {
                        sh 'npm test -- --passWithNoTests'
                    }
                }
            }
        }

        stage('Build application') {
            steps {
                script {
                    dir('frontend') {
                        // Build the Next.js application
                        sh 'npm run build'
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    echo 'Déploiement de l\'application'
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline terminé.'
        }
        success {
            echo 'Le pipeline a réussi !'
        }
        failure {
            echo 'Le pipeline a échoué.'
        }
    }
}
